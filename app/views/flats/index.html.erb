<div class="container">
  <div class="flats">
    <h2>All our Flats(<%= @flats.count %>)</h2>
      <% @flats.each do |flat| %>
        <div class="card-product">
          <h2><%= flat.name %></h2>
          <p><%= flat.address %></p>
          <p>😄</i><%= flat.number_of_guests %> 💰<%= flat.price_per_night %></p>
          <%= link_to  "More info", flat_path(flat) %>
          <%= link_to  "Edit", edit_flat_path(flat) %>
          <%= link_to  "Remove", flat_path(flat), method: :delete, data: { confirm: "Are you sure?" } %>
        </div>
      <% end %>
  </div>
</div>